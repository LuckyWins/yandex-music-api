# Dependencies
/vendor/

# Credentials. The committed twin is .env.local.example.
/.env.local

# Tool caches
/.phpunit.cache/
/.php-cs-fixer.cache

# Coverage reports, when make coverage is pointed at a directory instead of stdout
/coverage/

# Editors. Project-wide Claude settings (.claude/settings.json) are committed on
# purpose; only the personal overrides beside them are not.
/.idea/
/.vscode/
/.claude/settings.local.json

# macOS
.DS_Store
